# vagas-fronte123

## Project setup
```
npm install
```

### Compiles and hot-reloads for development
```
npm run serve
```

### Compiles and minifies for production
```
npm run build
```

### Lints and fixes files
```
npm run lint
```
### Git
```
git config --global user.name "Nome_usuário"
git config --global user.email email@usuario.com
git config --list
git status - verificar status
git remote -v - exibir repositorios
git remote add origin 'link do repositório' - vincular
git push -u origin master - enviar arquivos na branch master
git pull - atualizar repositório local de acordo com o remoto.
git clone 'link repositório a ser copiado'
```

### Customize configuration
See [Configuration Reference](https://cli.vuejs.org/config/).
